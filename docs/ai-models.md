# AI 模型与回退策略

## 原则

装备端必须能在模型不可用、校验失败或推理超时时继续运行。当前代码把规则和物理模型作为安全回退，模型只负责提高识别和预测质量，不承担唯一的过热保护职责。

## 模式分类器

说明书提出随机森林融合体温变化趋势和 IMU 姿态。对应的工程输入建议为：

| 特征 | 来源 | 窗口 |
| --- | --- | --- |
| 核心体温均值与斜率 | 体温传感器 | 10 秒、60 秒、5 分钟 |
| 表面温度变化 | NTC | 10 秒、60 秒 |
| 三轴加速度方差 | MPU6050 | 1 秒、5 秒 |
| 姿态角变化率 | 互补滤波 | 1 秒 |
| 静止持续时间 | 状态计时器 | 连续值 |
| 环境温度 | PT1000 | 10 秒均值 |

训练脚本 `tools/train_mode_classifier.py` 生成可复现的合成数据用于流水线验证。公开数据集或实采数据应在训练前进行受试者隔离切分，避免同一受试者的数据同时进入训练集和验证集。

必备指标：

- 昏迷召回率优先于总体准确率。
- 分受试者评估，而不是随机切分单帧。
- 报告误触发率、漏检率和从低温下降开始到识别的延迟。
- 在量化到 int8 后重新验证阈值附近的行为。

## 漂移预测

当前 `DriftPredictor` 使用海流矢量加风阻系数，适合作为 LSTM 的基线和链路失效回退。LSTM 输入可包括：

- 过去 1 小时的经纬度和速度，
- 海流东西、南北分量，
- 风速、风向和阵风，
- 水温与浪高，
- 初始位置和装备漂浮特性。

评估必须按时间切分，并以 1、3、6 小时的公里误差报告，不能只报告随机验证集损失。

## 电池 SOH

`BatteryHealthEstimator` 用容量比和内阻惩罚给出轻量估算。生产 SVM 可融合：

- 放电容量与额定容量比，
- 直流内阻增量，
- 不同温度下的电压平台变化，
- 充放电循环次数，
- 峰值电流和平均温度。

上线时需要设置保守回退：模型输出异常、超出训练分布或低于置信阈值时，采用容量表插值结果，并降低最大放电功率。

## 推理接口

服务端模型建议导出 ONNX，装备端建议使用 TensorFlow Lite Micro 或 CMSIS-NN。无论使用哪种 runtime，输入输出结构应与当前 Python 回退算法保持一致，以便逐帧对比。

